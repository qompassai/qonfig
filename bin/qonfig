#!/usr/bin/env python3
# /qompassai/qonfig/bin/qonfig
# Qompass AI Qonfig Binary
# Copyright (C) 2025 Qompass AI, All rights reserved
#################################################### 

# /qompassai/qonfig/bin/qonfig
# Qompass AI Qonfig Binary
# Copyright (C) 2025 Qompass AI, All rights reserved
####################################################
import sys, os

PROJECT_ROOT_DIRECTORY = os.path.dirname(
    os.path.dirname(os.path.realpath(__file__)))

def inject(lib_path):
    path = os.path.join(PROJECT_ROOT_DIRECTORY, 'lib', lib_path)
    sys.path.insert(0, path)

if sys.version_info[0] >= 3:
    inject('pyyaml/lib3')
else:
    inject('pyyaml/lib')

if os.path.exists(os.path.join(PROJECT_ROOT_DIRECTORY, 'qonfig')):
    if PROJECT_ROOT_DIRECTORY not in sys.path:
        sys.path.insert(0, PROJECT_ROOT_DIRECTORY)
        os.putenv('PYTHONPATH', PROJECT_ROOT_DIRECTORY)

import qonfig

def main():
    qonfig.cli.main()

if __name__ == '__main__':
    main()
